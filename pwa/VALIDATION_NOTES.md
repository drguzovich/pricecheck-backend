# Runtime Validation Notes

- The production PWA responded successfully on `/`, `/scan`, `/search`, `/result?barcode=6001069206154`, `/manifest.json`, `/sw.js`, and `/icons/icon.svg` during local runtime checks.
- Browser verification confirmed the dark mobile-first home page renders its navigation, scan/search calls to action, comparison preview, and recent-search links.
- On first result-page inspection, the loading state rendered as designed while the backend lookup was in progress.
- The completed result view rendered the product name, barcode, a Woolworths price of R24.95, the visible `Best price` rank label, cached-data freshness warning, and explicit `Not available` rows for Pick n Pay and Checkers.
- On 18 August 2026, the production result route was rechecked against the refreshed backend. It rendered `Rusks: Ouma Buttermilk, 500g`, barcode `6001069206154`, Woolworths `R24.95`, `Updated 8m ago`, and a visible `Best price` label. Pick n Pay and Checkers remained explicitly labelled `Not available`; no stale-data warning appeared because the Woolworths result was fresh.
- The same production browser session checked unknown barcode `0000000000000`. After the loading state, the PWA rendered `No listings found yet`, the barcode-specific API message, `Refresh prices`, and `Try another barcode`. It did not render the Ouma product or any mock retailer prices.
- On 19 August 2026, the production PWA was checked with live barcode `6001069600754`. It rendered `Ouma Buttermilk Rusks Sliced 450g`, a fresh Pick n Pay offer at `R63.99`, and a visible `Best price` label. Woolworths and Checkers remained explicit unavailable rows rather than disappearing from the comparison.
- After the Checkers and SPAR backend update deployed on 19 August 2026, the same live PWA route rendered all four retailer states. Checkers was ranked `Best price` at `R62.99`, Pick n Pay was ranked `2nd` at `R63.99`, and Woolworths plus SPAR were visible as `Not available` rows. This confirms sorted rank text, fresh timestamps, and explicit availability semantics in the production UI.
- The real phone-tested barcode `6009702444031` was rechecked after availability feedback was added. The PWA showed Checkers `R64.99` and now explained all non-price rows: Woolworths was not listed for the barcode, Pick n Pay had no exact barcode listing, and SPAR requires a store-specific price source. The view also reported `1 of 4 retailers currently have an exact barcode listing`.
- After real-device scanner feedback on 20 August 2026, the scanner was revised to request the rear camera automatically on page entry and use a horizontal, close-range barcode band. The browser sandbox correctly showed the direct camera-start attempt and its manual-entry fallback when camera access was unavailable. The result page also now states that retailer prices require exact barcode confirmation and that SPAR requires a store-specific source.
- The rebuilt production scanner route was rechecked after restart. It immediately attempted camera startup and did not present an `Open camera` control. In the browser sandbox, where camera access is unavailable, it cleanly displayed `Try camera again` and the manual-entry fallback; the compact horizontal barcode band and focus guidance remained visible.
- The comparison route was rechecked after its client timeout was increased from 16 to 35 seconds. The loading state now tells a tester that the first post-idle check may take up to 35 seconds while the test backend wakes up, rather than presenting the earlier premature timeout message.
- The no-data route for barcode `0000000000000` was verified in the production PWA. Rather than ending at a generic not-found screen, it displays an optional product-name/pack-size field, an `Add details` action, and a clear explanation that the information will make future exact retailer queries more precise.
